program findArray:
var int: i,s[10], result, find_val;

int func find(int x){
  s[5] = 5;
  s[4] = 9;
  s[3] = 7;
  s[1] = 2;
  s[2] = 0;
  s[0] = 11;
    for(i=0 to 5){
        if(s[i]==x){
          write("hola");
            return(i);
        };
    };
    return(404);
}

main(){
find_val=2;
result = find(find_val);
if(result == 404){
    write("El valor no se encuentra");
}
else{
    write("El valor se encontro en la posicion ", result); 
};
}